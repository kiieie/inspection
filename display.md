# AI Inspection Display Guidelines (점검 결과 디스플레이 설계서)

이 문서는 AI 점검 로직이 수행된 후, 사용자에게 보여지는 시각화(디스플레이) 결과물의 모든 요건과 규칙을 정의합니다. OpenCV를 활용한 시스템 화면부터 웹 기반 대시보드 화면까지 모두 포함하며, 화면 표출과 관련된 수정 사항 발생 시 **반드시 이 문서를 먼저 업데이트**해야 합니다.

## 목차 (Table of Contents)
1. [개요 (Overview)](#1-개요-overview)
2. [전체 디스플레이 플로우 (Display Flow)](#2-전체-디스플레이-플로우-display-flow)
3. [OpenCV 창 화면 (데스크톱 / 디버깅 UI)](#3-opencv-창-화면-데스크톱--디버깅-ui)
    - 3.1. 화면 요소 렌더링 방식
    - 3.2. 윈도우 창 레이아웃 및 제어 정책
4. [웹 대시보드 화면 (Web UI)](#4-웹-대시보드-화면-web-ui)
    - 4.1. 점검 사진 및 뷰어
    - 4.2. 점검 포인트 테이블 (Expected Points)
    - 4.3. AI 감지 결과 테이블 (Detected Results)
    - 4.4. 크로스 하이라이팅 연동 로직
5. [상태 및 색상 정의 (States & Palette)](#5-상태-및-색상-정의-states--palette)

---

## 1. 개요 (Overview)
AI 모델이 순찰 로봇(Spot 등)의 영상을 분석한 뒤 시각적인 가이드를 제공하여 관리자가 신속하게 진단을 검토할 수 있도록 합니다. 
핵심 철학은 **"이미지 자체에 검출 내역을 명확히 표시하고, 웹 화면에서는 상호작용을 통해 직관적으로 결과를 매칭"**하는 것입니다.

---

## 2. 전체 디스플레이 플로우 (Display Flow)

1. **로봇 순찰 데이터 유입**: 이미지가 수신되면 Task Queue에서 작업 시작
2. **AI(VLM/YOLO) 분석**:
   - 객체의 바운딩 박스(Bounding Box) 좌표 추출
   - 아날로그/디지털 게이지의 수치나 상태(PASS/FAIL) 판독
3. **Backend 렌더링 로직 (`main.py` / `visualizer.py`)**:
   - 추출된 좌표들에 대해 이미지 위에 다음 내용들을 렌더링함: **Bounding Box, 레이블, DG_* 결과값, AG_* 결과값(숫자)**
   - 정상 매칭된 항목은 초록/빨강/노랑, 매칭되지 않은 잉여 객체는 회색으로 그림
   - 합성된 이미지를 `.jpg` 파일로 저장 (웹 표시용)
   - `--withfig` 실행 옵션이 있다면 Local Desktop 표출 시 하나의 윈도우 창에 계속 나오게 갱신함
4. **웹 대시보드 표출 (`app.js` / `index.html`)**:
   - Backend가 저장한 `.jpg` 이미지를 **중앙에** 띄움
   - 하단 좌측엔 DB가 요구한 **검사 목록(Expected)**, 우측엔 AI가 실제 감지한 **검출 목록(Detected)** 표출
   - 사용자가 테이블의 특정 행을 클릭 시, 해당 좌표를 추적해 이미지 위에 노란색 보조 사각형(Highlight)을 입혀 강조
5. **DB 저장**: 공간 정보(`spatial_info`), 판정(`judgement`) 등 모든 정보를 DB(`InspectionResult`)에 적재

---

## 3. OpenCV 창 화면 (데스크톱 / 디버깅 UI)

현장 컴퓨터나 디버깅 툴로 직접 실행할 때(`python main.py --withfig`) 나타나는 팝업 창에 대한 정책입니다.

### 3.1. 화면 요소 렌더링 방식
이미지 상에 디스플레이 되는 세부 항목은 다음과 같습니다:
- **표시 내용**: **Bounding Box, 레이블, 판독값 (DG_*, AG_*, Class_ 결과값)**
  - `Class_` 모델의 경우에도, 감지 후 분류된 상태 텍스트(출력값)가 박스에 함께 명시되어야 합니다.
  - **오버랩 방지(겹쳐지지 않게)**: 텍스트 렌더링 시, 사진 내용이나 객체 박스와 글자가 겹쳐 식별이 불가능해지지 않도록 글자 배경색(검정 박스 배경 유틸리티 등)을 사용해 명시성을 확보해야 합니다.
- **박스 색상**:
  - `judgement == 'PASS'`: 🟢 초록색 테두리 (Thickness: 2)
  - `judgement == 'FAIL'`: 🔴 빨간색 테두리 (Thickness: 2)
  - `judgement == 'UNKNOWN'`: 🟡 노란색 테두리 (Thickness: 2)
  - **`Unmatched (미매칭)`**: ⚪ 회색 테두리 (Thickness: 1)
- **Unmatched 렌더링**: AI가 무언가 발견하긴 했으나, 기존 DB 검사 리스트와 포지션/이름이 일치하지 않아 버려진 탐지 객체들도 회색 박스로 그리며, 해당 객체들의 **레이블 및 판독값(AG_*, DG_*, Class_ 등)도 표시**해야 합니다.

### 3.2. 윈도우 창 레이아웃 및 제어 정책
- **단일 창 정책**: 윈도우 창 이름(`win_name`)을 고정 한 뒤 `cv2.setWindowTitle()`을 통해 타이틀을 동적으로 갱신합니다. 매번 새로운 창을 생성하고 소멸하면 Ubuntu/GNOME 등에서 "Window is ready" 알람 스팸이 발생하므로 이를 방지하기 위함입니다.
  ```python
  win_name = "AI Inspection Display"
  cv2.namedWindow(win_name, cv2.WINDOW_AUTOSIZE)
  win_title = f"[{task.site}] {task.mission_name} - {insp_name}"
  cv2.setWindowTitle(win_name, win_title)
  cv2.imshow(win_name, img)
  cv2.waitKey(1)
  ```
- **창 크기 정책**: `cv2.WINDOW_AUTOSIZE`를 사용하여 스캔된 카메라 이미지의 원본 해상도(비율) 그대로 창 크기가 맞춰집니다. 사용자가 마우스로 크기를 임의 조절하여 화면이 찌그러지는 것을 방지합니다.

---

## 4. 웹 대시보드 화면 (Web UI)

사용자가 최종적으로 결과를 심사(Review)하는 브라우저 UI(`app.js`, HTML)입니다.

### 4.1. 점검 사진 및 뷰어
- 백엔드에서 생성된 결과 이미지(Bounding Box, 레이블, DG/AG 결과값 포함)를 중앙에 100% Fit 비율로 꽉 차게 띄웁니다.
- 이미지 위에는 어떠한 별도의 **HTML Overlay Text도 없어야** 합니다. 사진 자체에 그려진 정보만을 온전히 보여줍니다.

### 4.2. 점검 포인트 테이블 (Expected Points)
- 왼쪽 하단 패널. **DB에서 읽어와서 현재 포인트에서 검사해야 하는 항목**입니다.
- **필수 출력 항목**: Type(기기 분류), Facility(설비 이름), Criteria(정상 범위/조건), **점검결과(DG_*, AG_* 판독값)**, **점검결과의 정상범위 여부(PASS/FAIL/등)**, Status(매칭 여부 Found/Not Found)

### 4.3. AI 감지 결과 테이블 (Detected Results)
- 오른쪽 하단 패널. AI가 해당 사진에서 **실제로 찝어낸** 모든 이력입니다.
- **표시 대상**: 왼쪽 테이블의 Expected Points와 매칭되어 검출된 항목들, 그리고 매칭되지 않고 버려진 미매칭 객체들(Unmatched)이 나열됩니다. **단, 왼쪽 테이블에서 'NOT Found' 처리된(AI가 아예 탐지조차 못한) 항목은 이 테이블에 나오지 않아야 합니다.**
- **미매칭 객체 표기 요건**: 
  - `Type` 표기 이름은 접두어를 제외하고 **"오인식된 원래 이름"**만 표시해 줍니다.
  - 리스트 상의 `Status` 상태 값을 **"Unmatched"**로 명시합니다.
  - 해당 객체의 **위치 정보(좌표)**는 목록 상에 제대로 띄워주어 하이라이트 클릭이 가능하도록 합니다.

### 4.4. 크로스 하이라이팅 연동 로직
사용자가 왼쪽 혹은 오른쪽 테이블의 한 `Row`를 클릭하면, 그와 연동된 테이블의 `Row`가 즉시 같이 선택되며(`active-row` 클래스 활성화), 이미지 상의 해당 좌표 위에 **가시성 높은 노란 테두리(`highlightItem(box)`)** 가 오버레이 됩니다.

**[예외 케이스 처리 정책]**
- **1:1 중복 객체 매칭 보장 (`data-type-idx`)**: 사진 내에 동일한 타입(예: `SW_Nobe_Right`)이 여러 개일 경우 엉뚱한 것이 선택되지 않도록, DOM 내부 리스트 렌더링 시 부여한 순번 표기 속성(`dataset.typeIdx`)을 서로 비교하여 정확히 추적해야 합니다.
- **`Not Found` 처리 항목 클릭 시**:
  - 관리자가 왼쪽 표에서 `Not Found` 행(AI가 못 찾음)을 클릭한 경우, 연동될 화면 상의 AI 감지 박스가 없으므로 하이라이트가 발생하지 않거나 0,0,0,0 좌표 처리로 아무것도 표시되지 않도록 처리합니다.

---

## 5. 상태 및 색상 정의 (States & Palette)

| 구분 | 목적 | 색상 (RGB/HEX) | 비고 |
|---|---|---|---|
| **PASS** | 정상 동작, 양호, 수치 범위 내 | 초록 `#00FF00` `(0,255,0)` | 안전상태 |
| **FAIL** | 비정상, 에러, 범위 초과 | 빨강 `#FF0000` `(0,0,255)`(CV2) | 위험/조치사항 |
| **UNKNOWN** | AI가 판정불가(흐림, 가려짐 등) | 노랑 `#FFFF00` `(0,255,255)`(CV2) | 수동검토 대상 |
| **Unmatched** | 매칭 대상이 없음, 남는 박스 | 회색 `#808080` `(128,128,128)` | 참고사항용 |
| **UI Highlight** | 웹 사용자가 테이블 클릭 시 추적용 | 형광노랑/오렌지 | CSS 제어 (`#FFEB3B` 등) |

> **주의:** OpenCV의 Python Color Array는 `(B, G, R)` 순서를 따릅니다.
